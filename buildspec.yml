phases:
  build:
    commands:
      - echo Build started on `date`
      - mvn test 
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
artifacts:
  files:
    - target/hello-world-1.0-SNAPSHOT.jar.original
    - appspec.yml
  discard-paths: yes